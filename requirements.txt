# 数据处理
numpy>=1.24
pandas>=2.0
pyarrow>=14.0  # 可选，parquet 二进制输出

# 机器学习（torch 单独安装，见 README）
scikit-learn>=1.3
statsmodels>=0.14

# 大模型 / 服务
openai>=1.0
fastapi>=0.110
uvicorn[standard]>=0.29
mcp>=1.0

# 时序数据库（本地 PostgreSQL 分区表）
psycopg2-binary>=2.9

# 文档导出（可选，生成 docx 用系统 python 即可）
# python-docx
